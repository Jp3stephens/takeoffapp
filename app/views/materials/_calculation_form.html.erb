<%= form_with(model: calculation, url: material_finish_path(@material), method: :post) do |form| %>
    <h1> Bruh </h1>
      <div class="actions">
        <%= form.submit 'Finish', class: 'btn btn-success' %>
      </div>
  <% end %>
  <h1> Bruv what </h1>
    